package dizilerders;

import java.util.Scanner;
/*
// diziler tanım : birden fazla degerin aynı tipte olmak sartıyla  tutuldugu yapılardır.
public class dizilerders1 {
    public static void main(String[] args) {
        /*
        int d[] = new int[3]; // dizi tanımlama a nın 3 elamı var anlamı var
        d[0]=, d[1], d[2];
        // deger verme ;
        //1 dogrudan kod ile deger verme ;
        d[0]=10;d[1]=20;d[2]=39;
        //2- klavyeden deger verme ( scanner)
        //3-dosyadan veriler alıp diziye aktarma sensorlerden web sitelerinden  (  PORT bilgileri)
        String s[] = new String[4];
       // string türünde kod içinde deger vereceksek
       s[0]="ali"; s[1]="veli";s[2]="a";s[3]="45"; // 45 sadece görüntü olarak ve cift tırnak kulanmak lazım .
       String g = s[2]; // degeri a dır .
        // char tipi degişkenlerde tek tırnaga dikkat edecegiz .
        char c [] = new char[3];
        c[0]='a'; c[1]='b'; c[2]= '4';
        char x = c[2] ; // dogru bir tabir ve cevap 4
        // stringlerde cift tırnak char tek tırnagı unutma !!
        // yarı dinamik tanım ; dizileri  başlangıc degerleriyle tanımlama :
        // int A[] = {1,2,3,10} elaman sayısı 4 diger sayı tiplerinde degişen yoktur.
        // float b[] = {3.123,112.21312}
        // String s [] = {"ali","veli","deli"} s[0]= "ali ";
        //char c []= {'veli '}
        // yüksek boyutlu dizilerde işlemler
        int a [][] = new int [2][3]; // 2 satır 3 sütündür
        /*
         4 6
      a= 5 1    a[0][0] = 4
         7 5


        String d1[][] = new String[2][3];
        Char v2[][] = new Char[2][4];
        // yüksek boyutlu dizileri bşlangıc degeriyle oluşturma
        int a[][] = {{4, 6}, {5, 1}, {7, 5}};
        //            alt taraf satır  üst taraf sütün
        String s1[][] = {{"ali", "veli"}, {"osman", "yusuf"}};// karakterlerde  tek tırnak (Char)
        //3 boyutlu dizilerin gösterimi
        int b[][][] = int [3][2][3];
        // 3 boyutllu dizziyi tanımlamaag
        int b[][][] = {{5, 6, 7}, {6, -1, 3}}, {
            {
                5, 6, 7
            },{
                6, -1, 3
            }
        },{
            {
                5, 6, 7
            },{
                6, -1, 3
            }
        } ;
        // dizi not1 :: bütün diziler sekli ne olursa olsun 0. elemandan başlar .
        // diziler not2 :: dizilerin en büyük avantajı kodlanmasının proglanmasının  kolay olmasınıdır.
        // not3 ::  dizilere hafızada dogrudan erişim söz konusunudur .
        //not4 ::  bir dizinin eleman sayısı bulmak bulmak için  lenght ifadesi kulanılır .
        int a[] = {5, 7, 8, 9, 0}; // statement kulanımı
        a.length // dogrudan elaman sayısını bulma
                // 2 boyutta  sütün sayısını bulmak için : a.[0].lenght a.lenght
                */


